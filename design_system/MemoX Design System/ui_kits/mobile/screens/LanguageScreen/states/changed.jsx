/* Language · state: changed — Vietnamese selected, restart banner shown. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Language = R.Language || {});
D.changed = function () { return { searching: false, changed: true }; };
})();
