/* Appearance · state: light — force light (Tokyo Pure). */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Appearance = R.Appearance || {});
D.light = function () { return { mode: 'light' }; };
})();
