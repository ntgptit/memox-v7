/* Onboarding · state: zero — zero-content Dashboard with 3 paths. Default. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Onboarding = R.Onboarding || {});
D.zero = function () { return {}; };
})();
