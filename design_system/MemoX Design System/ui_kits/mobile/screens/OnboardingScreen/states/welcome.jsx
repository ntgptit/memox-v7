/* Onboarding · state: welcome — one-time first-launch hero (full screen). */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Onboarding = R.Onboarding || {});
D.welcome = function () { return { welcome: true }; };
})();
