/* DeckImport · state: mapping — columns read; known header names auto-mapped (A11). ADDED in v3. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.DeckImport = R.DeckImport || {});
D.mapping = () => ({ kind: 'flow', fileChosen: true, mapping: true });
})();
