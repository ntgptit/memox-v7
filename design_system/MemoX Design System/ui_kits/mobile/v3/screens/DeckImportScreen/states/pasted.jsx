/* DeckImport · state: pasted — text pasted as the source (A11). ADDED in v3. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.DeckImport = R.DeckImport || {});
D.pasted = () => ({ kind: 'flow', pasted: true });
})();
