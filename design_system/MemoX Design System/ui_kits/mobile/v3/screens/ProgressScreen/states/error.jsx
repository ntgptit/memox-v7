/* Progress · state: error — read failed, retryable. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Progress = R.Progress || {});
D.error = () => ({ error: true });
})();
