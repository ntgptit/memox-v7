/* Progress · state: loaded — studied today, 7-day range. Default. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Progress = R.Progress || {});
D.loaded = () => ({ range: 'week' });
})();
