/* StudyHome · state: error — read failed, retryable. */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.StudyHome = R.StudyHome || {});
D.error = (ctx) => ctx.ErrorCard();
})();
