/* Language · state: changed — just switched to Vietnamese — applied at once (BR-215). */
(function () {
const R = (window.MemoXStates = window.MemoXStates || {});
const D = (R.Language = R.Language || {});
D.changed = () => ({ changed: true });
})();
