The one loading treatment in the app — a centred indigo spinner.

```jsx
<MxLoadingState semanticsLabel="Loading your decks" />
```

Never a skeleton: local reads finish in single-digit milliseconds, and motion that
says "slow" about something that is not is worse than a moment of nothing.
