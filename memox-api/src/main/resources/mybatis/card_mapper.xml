<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.memox.repository.CardRepository">

  <resultMap id="cardRow" type="com.memox.card.persistence.CardRow">
    <id column="id" property="id"/>
    <result column="deck_id" property="deckId"/>
    <result column="front" property="front"/>
    <result column="back" property="back"/>
    <result column="is_flagged" property="flagged"/>
    <result column="example" property="example"/>
    <result column="hint" property="hint"/>
    <result column="pronunciation" property="pronunciation"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <insert id="insertCard" parameterType="com.memox.card.persistence.CardRow">
    INSERT INTO cards (
      id, deck_id, front, back, front_folded, back_folded,
      example, hint, pronunciation, created_at, updated_at
    ) VALUES (
      #{id}, #{deckId}, #{front}, #{back}, #{frontFolded}, #{backFolded},
      #{example}, #{hint}, #{pronunciation}, #{createdAt}, #{updatedAt}
    )
  </insert>

  <insert id="insertInitialStudyState">
    INSERT INTO card_study_states (
      card_id, scheduler_type, scheduler_version, scheduler_generation
    ) VALUES (
      #{cardId}, #{schedulerType}, #{schedulerVersion}, #{schedulerGeneration}
    )
  </insert>

  <select id="findActiveCardsByDeck" resultMap="cardRow">
    SELECT id, deck_id, front, back, is_flagged, example, hint, pronunciation, created_at, updated_at
    FROM cards
    WHERE deck_id = #{deckId}
      AND delete_batch_id IS NULL
    ORDER BY created_at ASC, id ASC
    LIMIT #{page.limit}
    OFFSET #{page.offset}
  </select>

  <select id="countActiveCardsByDeck" resultType="long">
    SELECT COUNT(*)
    FROM cards
    WHERE deck_id = #{deckId}
      AND delete_batch_id IS NULL
  </select>
</mapper>
