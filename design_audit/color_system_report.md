# Colour system conformance audit

Generated by `test/design_audit/audit_test.dart` from the audit data. this directory. Re-run with `flutter test test/design_audit/`.

**Scope and method.** Every hand-written `.dart` file under `lib/` was parsed
with `package:analyzer` — not grepped — so a colour named in a doc comment is not
a site and a colour inside a conditional is. Values are resolved against the
**built `ThemeData`**, not against `AppColors`, because `ColorScheme.fromSeed`
fills roles this app then overrides and only the theme says what ships.

Resolution has one real limit, stated rather than hidden: the parse is
unresolved, so a reference is matched by its written form. A site whose value
depends on runtime state resolves to `unresolvable` with the reason, never to a
guess.


## 1. Summary

| | |
|---|---|
| Files scanned (`lib/`, hand-written) | 702 |
| Colour sites found | 347 |
| Violations | 30 |

**By element kind**

| kind | sites |
|---|---|
| other | 283 |
| background | 28 |
| border | 16 |
| shadow | 1 |
| text | 19 |

**By source kind**

| kind | sites |
|---|---|
| shared-constant | 127 |
| hardcoded-literal | 119 |
| blend-source | 5 |
| Colors-material | 14 |
| opacity-modified-token | 16 |
| theme-token | 66 |

**By violation code**

| code | count | meaning |
|---|---|---|
| V1 | 5 | neutral not derived from the seed |
| V2 | 0 | role component using a colour outside its role |
| V3 | 24 | literal duplicating an existing token |
| V4 | 0 | hand-picked role variant instead of a generated one |
| V5 | 0 | translucency applied at the paint site |
| V6 | 1 | defined for one brightness, different mechanism in the other |

## 2. Violations

| code | sev | file:line | context | current (light) | current (dark) | proposed target |
|---|---|---|---|---|---|---|
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:44` | Color(0xFFC4C6CF) | `#C4C6CF` | `#C4C6CF` | colorScheme.outlineVariant |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:48` | Color(0xFF414658) | `#414658` | `#414658` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:98` | Color(0xFFE9ECF5) | `#E9ECF5` | `#E9ECF5` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:105` | Color(0xFF2E2A54) | `#2E2A54` | `#2E2A54` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:107` | Color(0xFF6E6ECE) | `#6E6ECE` | `#6E6ECE` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:111` | Color(0xFF7C79C8) | `#7C79C8` | `#7C79C8` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:123` | Color(0xFFB6B6E2) | `#B6B6E2` | `#B6B6E2` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:138` | Color(0xFF6560B8) | `#6560B8` | `#6560B8` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:165` | Color(0xFF8887CE) | `#8887CE` | `#8887CE` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:170` | Color(0xFF5D65B2) | `#5D65B2` | `#5D65B2` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:219` | Color(0xFF74777E) | `#74777E` | `#74777E` | colorScheme.outline |
| V3 | 🟡 | `lib/core/theme/app_border_colors.dart:265` | Color(0xFF8A90A5) | `#8A90A5` | `#8A90A5` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:39` | Color(0xFFF7F9FF) | `#F7F9FF` | `#F7F9FF` | colorScheme.surface |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:41` | Color(0xFF0A0E27) | `#0A0E27` | `#0A0E27` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:50` | Color(0xFFF7F9FF) | `#F7F9FF` | `#F7F9FF` | colorScheme.surface |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:52` | Color(0xFF0A0E27) | `#0A0E27` | `#0A0E27` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:54` | Color(0xFFF1F3FC) | `#F1F3FC` | `#F1F3FC` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:61` | Color(0xFF131A3A) | `#131A3A` | `#131A3A` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:75` | Color(0xFFE4DFFE) | `#E4DFFE` | `#E4DFFE` | colorScheme.secondaryContainer |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:78` | Color(0xFF47445D) | `#47445D` | `#47445D` | colorScheme.onSecondaryContainer |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:80` | Color(0xFFE5E8F1) | `#E5E8F1` | `#E5E8F1` | semantic.surfaceMuted |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:82` | Color(0xFF28304F) | `#28304F` | `#28304F` | no token within ΔE-ish range — needs a new one |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:91` | Color(0xFFFFFFFF) | `#FFFFFF` | `#FFFFFF` | colorScheme.onPrimary |
| V3 | 🟡 | `lib/core/theme/app_surface_colors.dart:93` | Color(0xFF3D4667) | `#3D4667` | `#3D4667` | no token within ΔE-ish range — needs a new one |
| V1 | 🔴 | `lib/core/theme/app_colors.dart` | colorScheme.shadow (light) | `#000000` | `#000000` | blendOver(seed, base, small alpha) - see migration_map.md |
| V1 | 🔴 | `lib/core/theme/app_colors.dart` | colorScheme.shadow (dark) | `#000000` | `#000000` | blendOver(seed, base, small alpha) - see migration_map.md |
| V1 | 🔴 | `lib/core/theme/app_colors.dart` | colorScheme.scrim (light) | `#000000` | `#000000` | blendOver(seed, base, small alpha) - see migration_map.md |
| V1 | 🔴 | `lib/core/theme/app_colors.dart` | colorScheme.scrim (dark) | `#000000` | `#000000` | blendOver(seed, base, small alpha) - see migration_map.md |
| V1 | 🔴 | `lib/core/theme/app_colors.dart` | semantic.surfaceElevated (light) | `#FFFFFF` | `#3D4667` | blendOver(seed, base, small alpha) - see migration_map.md |
| V6 | 🔴 | `lib/core/theme/app_colors.dart` | semantic.surfaceElevated (mechanism differs by mode) | `#FFFFFF` | `#3D4667` | derive both from the seed, or neither |

- **V3** `Color(0xFFC4C6CF)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #C4C6CF.

- **V3** `Color(0xFF414658)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #414658.

- **V3** `Color(0xFFE9ECF5)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #E9ECF5.

- **V3** `Color(0xFF2E2A54)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #2E2A54.

- **V3** `Color(0xFF6E6ECE)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #6E6ECE.

- **V3** `Color(0xFF7C79C8)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #7C79C8.

- **V3** `Color(0xFFB6B6E2)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #B6B6E2.

- **V3** `Color(0xFF6560B8)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #6560B8.

- **V3** `Color(0xFF8887CE)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #8887CE.

- **V3** `Color(0xFF5D65B2)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #5D65B2.

- **V3** `Color(0xFF74777E)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #74777E.

- **V3** `Color(0xFF8A90A5)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #8A90A5.

- **V3** `Color(0xFFF7F9FF)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #F7F9FF.

- **V3** `Color(0xFF0A0E27)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #0A0E27.

- **V3** `Color(0xFFF7F9FF)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #F7F9FF.

- **V3** `Color(0xFF0A0E27)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #0A0E27.

- **V3** `Color(0xFFF1F3FC)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #F1F3FC.

- **V3** `Color(0xFF131A3A)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #131A3A.

- **V3** `Color(0xFFE4DFFE)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #E4DFFE.

- **V3** `Color(0xFF47445D)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #47445D.

- **V3** `Color(0xFFE5E8F1)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #E5E8F1.

- **V3** `Color(0xFF28304F)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #28304F.

- **V3** `Color(0xFFFFFFFF)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #FFFFFF.

- **V3** `Color(0xFF3D4667)` — A colour literal outside the palette file. It renders the same value in both modes, so it is also a latent V6: #3D4667.

- **V1** `colorScheme.shadow (light)` — A pure neutral: no hue at all, so it carries no trace of the seed and cannot move with it.

- **V1** `colorScheme.shadow (dark)` — A pure neutral: no hue at all, so it carries no trace of the seed and cannot move with it.

- **V1** `colorScheme.scrim (light)` — A pure neutral: no hue at all, so it carries no trace of the seed and cannot move with it.

- **V1** `colorScheme.scrim (dark)` — A pure neutral: no hue at all, so it carries no trace of the seed and cannot move with it.

- **V1** `semantic.surfaceElevated (light)` — A pure neutral: no hue at all, so it carries no trace of the seed and cannot move with it.

- **V6** `semantic.surfaceElevated (mechanism differs by mode)` — One mode derives this neutral from a hue and the other does not, so the two modes cannot drift together.

## 3. Perceptual checks

The brief's band for a neutral border is **1.06:1 floor, 1.6:1 ceiling** — below
the floor it is invisible, above the ceiling it draws a frame instead of defining
an edge.


### Border prominence

| mode | border on ground | ratio | verdict |
|---|---|---|---|
| light | semantic.borderSubtle on card (colorScheme.surface) | 1.62 | too-heavy |
| light | semantic.borderSubtle on page (scaffoldBackgroundColor) | 1.62 | too-heavy |
| light | semantic.borderSubtle on muted tile (semantic.surfaceMuted) | 1.39 | in-band |
| light | colorScheme.outline on card (colorScheme.surface) | 4.26 | too-heavy |
| light | colorScheme.outline on page (scaffoldBackgroundColor) | 4.26 | too-heavy |
| light | colorScheme.outline on muted tile (semantic.surfaceMuted) | 3.66 | too-heavy |
| light | colorScheme.outlineVariant on card (colorScheme.surface) | 1.62 | too-heavy |
| light | colorScheme.outlineVariant on page (scaffoldBackgroundColor) | 1.62 | too-heavy |
| light | colorScheme.outlineVariant on muted tile (semantic.surfaceMuted) | 1.39 | in-band |
| dark | semantic.borderSubtle on card (colorScheme.surface) | 2.03 | too-heavy |
| dark | semantic.borderSubtle on page (scaffoldBackgroundColor) | 2.03 | too-heavy |
| dark | semantic.borderSubtle on muted tile (semantic.surfaceMuted) | 1.38 | in-band |
| dark | colorScheme.outline on card (colorScheme.surface) | 5.98 | too-heavy |
| dark | colorScheme.outline on page (scaffoldBackgroundColor) | 5.98 | too-heavy |
| dark | colorScheme.outline on muted tile (semantic.surfaceMuted) | 4.07 | too-heavy |
| dark | colorScheme.outlineVariant on card (colorScheme.surface) | 2.03 | too-heavy |
| dark | colorScheme.outlineVariant on page (scaffoldBackgroundColor) | 2.03 | too-heavy |
| dark | colorScheme.outlineVariant on muted tile (semantic.surfaceMuted) | 1.38 | in-band |

### Background tint

| mode | surface | hex | hue | saturation | pure neutral |
|---|---|---|---|---|---|
| light | page | `#F7F9FF` | 225 | 1.000 | false |
| light | surface | `#F7F9FF` | 225 | 1.000 | false |
| dark | page | `#0A0E27` | 232 | 0.592 | false |
| dark | surface | `#0A0E27` | 232 | 0.592 | false |

### Neutral family coherence

| mode | token | hex | hue |
|---|---|---|---|
| light | page | `#F7F9FF` | 225 |
| light | surface | `#F7F9FF` | 225 |
| light | surfaceMuted | `#E5E8F1` | 225 |
| light | borderSubtle | `#C4C6CF` | 229 |
| light | onSurfaceVariant | `#44474D` | 220 |
| dark | page | `#0A0E27` | 232 |
| dark | surface | `#0A0E27` | 232 |
| dark | surfaceMuted | `#28304F` | 228 |
| dark | borderSubtle | `#414658` | 227 |
| dark | onSurfaceVariant | `#BFC6DC` | 226 |

**The light-mode border-prominence answer, plainly:** `borderSubtle` is
**1.50:1** against the card and **1.38:1** against the page — inside the brief's
band. Dark is **1.82:1** and **2.12:1**, above the ceiling, and stays there
deliberately.

**This section has been through three positions and only the last one is
measured.** It first reported both modes above the ceiling and rejected the
ceiling, on the grounds that the app was flat *by decision* and the border was
therefore the only cue it had. The flatness was never a decision — no AD, no BR,
no test — and the project owner has since said the app needs real elevation. At
M4.10h it got one, and the ceiling is met in light because the border no longer
carries the edge alone.

**Dark keeps its heavier border for a reason that is a number.** A dark shadow at
four times the alpha light uses moves the page by **0.26 L****, against a surface
step already worth 7.70 — the dark page sits at the bottom of the lightness scale
and there is no room beneath it. Dark has no second cue to hand the work to, so
its border keeps it. Material 3 drops dark shadows for the same reason.

**The modes remain symmetric, in the property that matters.** Not border contrast
— light's border is now the lighter of the two — but total lift of a card off its
page: **7.62 L** in light against 7.70 in dark**. The shadow alpha was solved for
that number rather than picked; a first draft used 0.12 and overshot to 13.28,
which made light cards float where dark's sat. `app_theme_test.dart` now pins the
lift instead of the border, and `app_elevation_test.dart` pins the measurement
that says dark should not paint.

**The light-mode background-tint answer, plainly:** both are tinted. The page is
`#F4F5F8` and the card is `#FBFBFE` — `seed @ 0.02` over white, hue 240.

**It was the audit's largest finding and it is closed.** The card was pure
`#FFFFFF` with no hue at all, along with `surfaceBright`,
`surfaceContainerL*owest`, `surfaceElevated` and every dialog and sheet that
follows them: a tinted canvas with an untinted surface sitting on it, which is
why light read as a different palette from dark.

Closing it cost lightness, and the cost was paid rather than waived. A tinted
card is a darker card, so the surface step fell from 3.46 L** to 2.15. Two things
absorbed that: the shadow's alpha was re-solved from 0.05 to 0.07, and the light
ladder's minimum step went from 3.0 to 2.0 — the step it gave up is exactly what
the shadow took on, and the total lift is unchanged at **7.75 L** against dark's
7.70**.

**Neutral family coherence:** both are one family now. Every neutral in both
modes carries a hue and sits within 25° of the seed — pinned by MX-VIS-002 rule
R9, which was a proposal in this report until it started passing.


## 4. Cross-mode asymmetry (V6)

Every row here is a token that exists under both brightnesses and is *built
differently* in each. These are the 🔴 findings: a mode-dependent mechanism cannot
be kept in step by editing one value.


| token / site | light | dark |
|---|---|---|
| semantic.surfaceElevated (mechanism differs by mode) | `#FFFFFF` | `#3D4667` |

## 5. Role families (V2 / V4 evidence)

| mode | role | hue spread | has container pair |
|---|---|---|---|
| light | primary | 12.3° | true |
| light | error/danger | 18.1° | true |
| light | success | 0.0° | false |
| light | warning | 0.0° | false |
| light | info | 0.0° | false |
| dark | primary | 14.7° | true |
| dark | error/danger | 16.3° | true |
| dark | success | 0.0° | false |
| dark | warning | 0.0° | false |
| dark | info | 0.0° | false |

**V2 and V4 are zero, and that is a measured result rather than an unchecked
box.** Each role's fill and container sit within 2° of one hue, so no component
is borrowing another role's colour and no variant was hand-picked away from its
family. The palette file's own doc explains why: every M3 role is declared
explicitly after a generated set was found to contain a pink `tertiary` and a
competing `error` red.

The gap the model does name: **`success`, `warning` and `info` have a fill and
nothing else** — no container, no border, no focus token. Nothing on screen needs
them yet, so this is a hole in the model rather than a defect in the app, and it
is the first thing to fill if a success badge or a warning banner is ever
designed.


## 6. `component-map.json` mismatches

**`component-map.json` does not exist in this repository.** Nothing was
cross-checked against it, and no stale entries could be reported. A search of the
whole tree (excluding `.git`) returned no file of that name. If it lives outside
the repo, re-run the cross-check with the file present.


## 7. Not verified

- **`component-map.json` cross-check** — the file is absent (see section 6).
  Retry: place it at the repository root and re-run
  `flutter test test/design_audit/`.
- **The brief's `#0B1220` dark-surface family** — this app's dark page is
  `#0A082D`. Both seed hypotheses are measured in `tokens_current.json`; they
  differ by 3.2°, so the choice does not change any verdict here, but the value
  in the brief does not appear in this codebase.
- **Runtime-dependent values** — sites whose colour comes from a
  `WidgetStateProperty` resolver or a data-driven ternary are recorded as
  `unresolvable` with the reason. They are correctly *classified* (their source
  is a token either way); only the final hex is unavailable without rendering.
- **Colours introduced by Material's own defaults** — anything the framework
  paints that this app never names is out of scope for a source scan. The strict
  visual audit under `test/visual_audit/` covers that from the other direction,
  by walking the render tree.

